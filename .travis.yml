language: java

jdk:
  - oraclejdk8

sudo: false

install: mvn install -DskipTests=true -Dmaven.javadoc.skip=true -B -V
script: mvn clean verify -B

cache:
  directories:
    - ~/.m2/repository

after_success:
  - ./travis-ci/deploy.sh